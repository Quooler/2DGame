/* Pixels per unit tienen que ser iguales en todos los sprites para que no haya problemas de escala. 
 * 
 * Pivote de los players abajo (bottom), pivote de las tiles arriba (top).
 * 
 * Sorting layer define el orden de dibujado de los sprites a nivel de profundidad-. 
 * 
 * Type full rect para las tiles.
 * 
 * */

